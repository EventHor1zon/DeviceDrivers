set(srcs
    "DeviceRegistry.c"   
)

set(includes "inc/")

# Uncomment to make this an ESP component!
# idf_component_register(SRCS "${srcs}"
#                     INCLUDE_DIRS ${includes}
# REQUIRES "esp_event" "driver" 
# )
